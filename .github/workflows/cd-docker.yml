# name: CD - Deploy to Azure

# on:
#   push:
#     branches:
#       - main
#     paths:
#       - 'src/**'
#       - 'infra/**'
#       - '.github/workflows/cd-docker.yml'
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Deployment environment'
#         required: true
#         default: 'development'
#         type: choice
#         options:
#           - development
#           - staging
#           - production

# env:
#   AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
#   AZURE_RESOURCE_GROUP: 'Eshop-wus-rg'
#   AZURE_LOCATION: 'westus'

# jobs:
#   deploy-infrastructure:
#     name: Deploy Infrastructure
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       id-token: write
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4

#     - name: Azure Login
#       uses: azure/login@v1
#       with:
#         client-id: ${{ secrets.AZURE_CLIENT_ID }}
#         tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#         subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

#     - name: Deploy Key Vault using Bicep
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/keyvault.bicep \
#           --parameters infra/keyvault.parameters.json

#     - name: Deploy App Insights using Bicep
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/appinsights.bicep \
#           --parameters infra/appinsights.parameters.json

#     - name: Run policy compliance check
#       run: |
#         az deployment group what-if \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/main.bicep \
#           --parameters infra/main.parameters.json

#     - name: Security compliance scan
#       run: |
#         az security assessment list \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --query "[].{Name:displayName, Status:status.code}"

#   deploy-dev:
#     name: Deploy to Development
#     needs: deploy-infrastructure
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       id-token: write
#     environment:
#       name: development
#       url: https://eshop-dev.azurewebsites.net
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4

#     - name: Azure Login
#       uses: azure/login@v1
#       with:
#         client-id: ${{ secrets.AZURE_CLIENT_ID }}
#         tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#         subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

#     - name: Deploy App Service (Dev)
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/webapp-docker.bicep \
#           --parameters infra/webapp-docker-dev.parameters.json

#     - name: Add role assignment (Dev)
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/webapp-to-acr-roleassignment.bicep

#     - name: Smoke test
#       run: |
#         sleep 30
#         curl -f https://eshop-dev.azurewebsites.net/health || exit 1

#   deploy-staging:
#     name: Deploy to Staging
#     needs: deploy-dev
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       id-token: write
#     environment:
#       name: staging
#       url: https://eshop-staging.azurewebsites.net
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4

#     - name: Azure Login
#       uses: azure/login@v1
#       with:
#         client-id: ${{ secrets.AZURE_CLIENT_ID }}
#         tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#         subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

#     - name: Deploy App Service (Staging)
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/webapp-docker.bicep \
#           --parameters infra/webapp-docker-staging.parameters.json

#     - name: Add role assignment (Staging)
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/webapp-to-acr-roleassignment.bicep

#     - name: Run security scan
#       run: |
#         echo "Running OWASP ZAP or similar pen-test against staging..."
#         # az container create --resource-group ${{ env.AZURE_RESOURCE_GROUP }} --name zap --image owasp/zap2docker-stable

#     - name: Smoke test
#       run: |
#         sleep 30
#         curl -f https://eshop-staging.azurewebsites.net/health || exit 1

#   deploy-production:
#     name: Deploy to Production
#     needs: deploy-staging
#     runs-on: ubuntu-latest
#     if: github.ref == 'refs/heads/main'
#     permissions:
#       contents: read
#       id-token: write
#     environment:
#       name: production
#       url: https://eshop.azurewebsites.net
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4

#     - name: Azure Login
#       uses: azure/login@v1
#       with:
#         client-id: ${{ secrets.AZURE_CLIENT_ID }}
#         tenant-id: ${{ secrets.AZURE_TENANT_ID }}
#         subscription-id: ${{ env.AZURE_SUBSCRIPTION_ID }}

#     - name: Deploy App Service (Production)
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/webapp-docker.bicep \
#           --parameters infra/webapp-docker-prod.parameters.json

#     - name: Add role assignment (Production)
#       run: |
#         az deployment group create \
#           --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
#           --template-file infra/webapp-to-acr-roleassignment.bicep

#     - name: Health check
#       run: |
#         sleep 30
#         curl -f https://eshop.azurewebsites.net/health || exit 1

#     - name: Notify deployment
#       if: success()
#       run: |
#         echo "Production deployment successful!"
